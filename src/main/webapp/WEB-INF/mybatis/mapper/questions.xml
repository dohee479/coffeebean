<?xml version="1.0" encoding="euc-kr" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mycompany.webapp.dao.QuestionsDao">

	<!-- 상품 정보에 대한 전체 문의 --> 
	<select id="selectProduct" parameterType="int" resultType="question">
		select * 
		from question
		where product_product_id = #{product_product_id}
		order by question_id desc
	</select>
	
	<!-- 사용자 my 문의 
		유저의 id에 해당하는 question 목록들을 찾아 내림차순으로 정리
	-->
	<select id="selectUser" parameterType="int" resultType="question">
		select *
		from question
		where users_user_id=#{users_user_id}
		order by question_id desc
	</select>
	
	<update id="update" parameterType="int">
		update boards set
			question_title=#{question_title},
			question_content=#{question_content}
		where question_id = #{question_id}
	</update>
	
	<delete id="delete" parameterType="int">
	</delete>
</mapper>
